const DEFAULT_INDENTATION = 2;
export default function writeJSON(filepath, contents, replacer, space) {
    return this.write(filepath, JSON.stringify(contents, 
    // Convert to any due to multiples stringify signatures.
    replacer, space || DEFAULT_INDENTATION) + '\n');
}
