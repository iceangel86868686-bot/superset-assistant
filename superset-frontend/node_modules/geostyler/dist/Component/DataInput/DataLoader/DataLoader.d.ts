import { default as React } from 'react';
import { VectorData, DataParser } from 'geostyler-data';
import { UploadButtonProps } from '../../UploadButton/UploadButton';
export interface DataLoaderProps {
    /** The callback method that is triggered when the state changes */
    onDataRead?: (data: VectorData) => void;
    /** List of data parsers to use */
    parsers: DataParser[];
    /** Properties that get passed to the UploadButton. */
    uploadButtonProps?: UploadButtonProps;
}
export declare const DataLoader: React.FC<DataLoaderProps>;
