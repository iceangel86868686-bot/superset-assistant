import { jsx as a } from "react/jsx-runtime";
import { Radio as r } from "antd";
import c from "lodash-es/get.js";
import { useGeoStylerLocale as u } from "../../../../context/GeoStylerContext/GeoStylerContext.js";
const M = ({
  colorMapTypeOptions: o = ["ramp", "intervals", "values"],
  onChange: t,
  value: l
}) => {
  const p = u("ColorMapTypeField"), n = o.map((e) => /* @__PURE__ */ a(
    r.Button,
    {
      value: e,
      children: c(p, `${e}MapTypeLabel`)
    },
    e
  )), s = (e) => {
    const m = e.target.value;
    t && t(m);
  }, i = l || o[0];
  return /* @__PURE__ */ a(
    r.Group,
    {
      className: "color-map-type-field",
      defaultValue: i,
      buttonStyle: "solid",
      onChange: s,
      size: "small",
      children: n
    }
  );
};
export {
  M as ColorMapTypeField
};
